{% extends "layouts/app.twig" %}

{% block title %} Users {% endblock %}

{% block content %}
    {# <h1>{{ user.username }}</h1>
    <ul>
        <li>{{ user.name }}</li>
        <li>{{ user.email }}</li>
    </ul> #}

    <ul>
        {% for user in users %}
            <li>{{ user.username }}</li>
        {% endfor %}
    </ul>
{% endblock %}